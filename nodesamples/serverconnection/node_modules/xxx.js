const express=require("express")
const app=express();
const Awr=(req,res,next)=>{
    const{username,password}=req.query;
    const Auser=users.find((user)=>user.username===username && user.password==password);
    if(Auser){
        next();
    }
    else{
        res.status(401).send('unaouthorised');
    }
};
const port=80;
app.listen(port,()=>{
    console.log(`node connected in port 80 ${port}`);
});
app.get('proctected', Awr, (req, res) =>{

    res.send('protected route : you are authenticated!');

});